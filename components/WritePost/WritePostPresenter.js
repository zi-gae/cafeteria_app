import React from "react";
import { Text, View } from "react-native";

const WritePostPresenter = ({ params }) => (
  <View>
    <Text>WritePostPresenter</Text>
  </View>
);

export default WritePostPresenter;
