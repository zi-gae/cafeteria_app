import React from "react";
import { Text, View } from "react-native";

const LibraryPresenter = ({ params }) => (
  <View>
    <Text>LibraryPresenter</Text>
  </View>
);

export default LibraryPresenter;
